@model IEnumerable<CMS2.Models.Keyword>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.keyword1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.keyword1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.keywordId }) |
            @Html.ActionLink("Details", "Details", new { id=item.keywordId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.keywordId })
        </td>
    </tr>
}

</table>
